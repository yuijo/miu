# built-in TCP input
# $ echo <json> | fluent-cat <tag>
<source>
  type forward
</source>

# miu default rpc input
<source>
  type msgpack_rpc
  bind localhost
  port <%= Miu.default_rpc_port %>
</source>
